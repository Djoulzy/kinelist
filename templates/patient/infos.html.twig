
<div class="row">
    <div class="col-5">
        <div class="row">
            <div class="col-5">Date de naissance:</div><div class="col-7 text-important">{{ patient.dateNaissance ? patient.dateNaissance|date('d/m/Y') : '' }}</div>
        </div>
        <div class="row">
            <div class="col-5">RNIPP:</div><div class="col-7 text-important">{{ patient.secu }}</div>
        </div>
        <div class="row">
            <div class="col-5">Sexe:</div><div class="col-7 text-important">{{ patient.sexe }}</div>
        </div>
    </div>
    <div class="col-5">
        <div>Adresse:</div>
        <div class="text-important">{{ patient.adresse|nl2br }}</div>
    </div>
    <div class="col-2">
        <div class="py-1"><button type="button" class="action_button btn btn-primary btn-xs btn-block" data-href="{{ path('patient_edit', {'id': patient.id}) }}" data-target="details_patient">Modifier</button></div>
        <div class="py-1">{{ include('patient/_delete_form.html.twig') }}</div>
    </div>
</div>
