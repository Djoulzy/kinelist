<h1>Pathologie</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ pathologie.id }}</td>
        </tr>
        <tr>
            <th>Label</th>
            <td>{{ pathologie.label }}</td>
        </tr>
    </tbody>
</table>

<button type="button" class="action_button btn btn-primary" data-href="{{ path('pathologie_index', {'patient_id': patient.id}) }}">back to list</button>

<button type="button" class="action_button btn btn-primary" data-href="{{ path('pathologie_edit', {'patient_id': patient.id, 'id': pathologie.id}) }}">edit</button>

{{ include('pathologie/_delete_form.html.twig') }}
